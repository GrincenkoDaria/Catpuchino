(() => {
    const STORAGE_KEY = 'siteLang';
    const DEFAULT_LANG = 'en';
    const TRANSITION_MS = 220;
    const TRANSLATABLE_ATTRS = ['placeholder', 'aria-label', 'title'];

    const EN_TO_SK = {
        'Home': 'Domov',
        'Adopt': 'Adopcia',
        'Menu': 'Menu',
        'Events': 'Udalosti',
        'Merch': 'Merch',
        'About us': 'O nás',
        'About Us': 'O nás',
        'Contact': 'Kontakt',
        'Social Media': 'Sociálne siete',
        'Email:': 'Email:',
        'Phone:': 'Telefón:',
        'Address:': 'Adresa:',
        'Hours:': 'Otváracie hodiny:',
        'Mon-Sun, 9:00 AM - 9:00 PM': 'Po-Ne, 9:00 - 21:00',
        'All rights reserved.': 'Všetky práva vyhradené.',
        'Donate': 'Darovať',
        'Open navigation': 'Otvoriť navigáciu',
        'Toggle navigation': 'Prepnúť navigáciu',
        'Mobile quick navigation': 'Rýchla mobilná navigácia',
        'Close': 'Zavrieť',

        'YOUR COFFEE BREAKCOULD SAVE A LIFE.': 'TVE PRESTÁVKA NA KÁVU MÔŽE ZACHRÁNIŤ ŽIVOT.',
        'YOUR COFFEE BREAK': 'TVOJA PRESTÁVKA NA KÁVU',
        'COULD SAVE A LIFE.': 'MÔŽE ZACHRÁNIŤ ŽIVOT.',
        'Enjoy advanced coffee & find your new best friend.': 'Vychutnajte si skvelú kávu a nájdite si nového najlepšieho priateľa.',

        'Shelter Partnership': 'Spolupráca s útulkom',
        'Want to adopt a cat?': 'Chcete adoptovať mačku?',
        'We work with a local animal shelter to bring adoptable cats to Catppuccino Café. Meet our cats, learn their personalities, and find the right companion.': 'Spolupracujeme s miestnym útulkom, aby sme priniesli mačky na adopciu do Catppuccino Café. Spoznajte naše mačky, ich povahy a nájdite si správneho spoločníka.',
        'Adoptable Cats': 'Mačky na adopciu',
        'Adopted Cats': 'Adoptované mačky',
        'Featured Cats': 'Vybrané mačky',
        'Learn more': 'Zistiť viac',
        'Adopted': 'Adoptované',
        'Adopt me': 'Adoptuj ma',
        'Age:': 'Vek:',

        'Drinks': 'Nápoje',
        'Desserts': 'Dezerty',
        'Discover café favorites made fresh daily.': 'Objavte obľúbené kaviarenské nápoje pripravované každý deň.',
        'Sweet treats that pair perfectly with your coffee.': 'Sladké dobroty, ktoré sa dokonale hodia ku káve.',
        'No items available right now.': 'Momentálne nie sú dostupné žiadne položky.',
        'No Image': 'Bez obrázka',

        'Upcoming Events': 'Nadchádzajúce udalosti',
        'Previous Events': 'Predchádzajúce udalosti',
        'No events available right now.': 'Momentálne nie sú dostupné žiadne udalosti.',

        'Merchandise': 'Merchandise',
        'Add to Cart': 'Pridať do košíka',
        'In stock:': 'Na sklade:',
        'Out of stock': 'Vypredané',
        'Your Cart': 'Váš košík',
        'Your cart is empty.': 'Váš košík je prázdny.',
        'Total:': 'Spolu:',
        'Order': 'Objednať',
        'Sorry': 'Prepáčte',
        'This item is out of stock.': 'Táto položka je vypredaná.',

        'Checkout': 'Pokladňa',
        'Your order': 'Vaša objednávka',
        'Name': 'Meno',
        'Email': 'Email',
        'Address': 'Adresa',
        'Payment': 'Platba',
        'Cash': 'Hotovosť',
        'Card': 'Karta',
        'Card Number': 'Číslo karty',
        'Expiry': 'Platnosť',
        'Confirm Order': 'Potvrdiť objednávku',
        'Back to shop': 'Späť do obchodu',
        'Thank you 😻': 'Ďakujeme 😻',
        'Your order was successful!': 'Vaša objednávka bola úspešná!',

        'About Catppuccino': 'O Catppuccino',
        'A cat café built around comfort, community, and second chances.': 'Mačacia kaviareň postavená na pohodlí, komunite a druhých šanciach.',
        'We bring together specialty coffee, a calm café atmosphere, and adoption awareness so every visit supports cats looking for a home.': 'Spájame výberovú kávu, pokojnú atmosféru kaviarne a osvetu adopcie, aby každá návšteva pomáhala mačkám hľadajúcim domov.',
        'What We Do': 'Čo robíme',
        'We create a welcoming space for guests and adoptable cats.': 'Vytvárame prívetivý priestor pre hostí a mačky na adopciu.',
        'Café Experience': 'Zážitok z kaviarne',
        'We serve handcrafted drinks and desserts in a relaxed environment designed for cozy visits and meaningful downtime.': 'Podávame ručne pripravované nápoje a dezerty v uvoľnenom prostredí pre príjemné návštevy a oddych.',
        'Adoption Support': 'Podpora adopcie',
        'Our team works with local shelter partners to introduce guests to adoptable cats and encourage informed, responsible adoptions.': 'Náš tím spolupracuje s miestnymi útulkami, aby hostí zoznámil s mačkami na adopciu a podporil zodpovedné rozhodnutia.',
        'Daily Enrichment': 'Denné obohatenie',
        'Each cat has a structured routine with rest areas, play sessions, and spaces that support safe, low-stress interactions.': 'Každá mačka má režim s oddychovými zónami, hrou a priestorom pre bezpečné interakcie s nízkym stresom.',
        'Our Supporters': 'Naši podporovatelia',
        'The café runs because a wider community believes in the mission.': 'Kaviareň funguje vďaka širšej komunite, ktorá verí v naše poslanie.',
        'Local Shelters': 'Miestne útulky',
        'They help us match cats with guests, coordinate care plans, and keep adoption standards strong.': 'Pomáhajú nám prepájať mačky s hosťami, koordinovať starostlivosť a udržiavať vysoký štandard adopcie.',
        'Regular Guests': 'Stáli hostia',
        'Every coffee, dessert, and return visit helps fund enrichment supplies, foster transitions, and shelter support.': 'Každá káva, dezert a opätovná návšteva pomáha financovať potreby pre mačky, dočasnú starostlivosť a podporu útulkov.',
        'Independent Makers': 'Nezávislí tvorcovia',
        'Merch collaborators, event hosts, and neighborhood partners help us build a stronger cat-loving community.': 'Partneri v merchi, organizátori podujatí a susedské spolupráce nám pomáhajú budovať silnejšiu komunitu milovníkov mačiek.',
        'Our Location': 'Naša poloha',
        'Find the café and view the store location below.': 'Nájdite kaviareň a pozrite si jej polohu nižšie.',
        'Coordinates:': 'Súradnice:',
        'Catppuccino Café location map': 'Mapa polohy Catppuccino Café',
        'Catppuccino Café location preview map': 'Náhľad mapy polohy Catppuccino Café',

        'Support our café cats and help us care for them.': 'Podporte naše kaviarenské mačky a pomôžte nám starať sa o ne.',
        'Please fix these errors:': 'Prosím, opravte tieto chyby:',
        'Donation amount': 'Suma daru',
        'First name': 'Meno',
        'Last name': 'Priezvisko',
        'Card number': 'Číslo karty',
        'Send': 'Odoslať',
        'Thank you for your donate,': 'Ďakujeme za váš dar,',
        'Go back': 'Späť',

        'Donation amount must be between 5 and 100 €.': 'Suma daru musí byť medzi 5 a 100 €.',
        'First name is not in the correct format.': 'Meno nie je v správnom formáte.',
        'Last name is not in the correct format.': 'Priezvisko nie je v správnom formáte.',
        'Card number must contain exactly 16 digits.': 'Číslo karty musí obsahovať presne 16 číslic.',
        'Expiry must be in MM/YY format.': 'Platnosť musí byť vo formáte MM/RR.',
        'Card expiry date is in the past.': 'Platnosť karty je v minulosti.',
        'CVV must contain 3 or 4 digits.': 'CVV musí obsahovať 3 alebo 4 číslice.',
        'Failed to save donation.': 'Dar sa nepodarilo uložiť.',
        'Database error.': 'Chyba databázy.',

        'To Be Done': 'Na doplnenie',
        'This part is not implemented yet.': 'Táto časť ešte nie je dokončená.',
        'This feature/page still needs to be built. You can update this message in': 'Táto funkcia/stránka ešte musí byť vytvorená. Túto správu môžete upraviť v',
        'anytime.': 'kedykoľvek.',
        'Requested feature:': 'Požadovaná funkcia:',
        'Back Home': 'Späť domov',
        'Go Back': 'Späť',

        'Thank you for your order': 'Ďakujeme za vašu objednávku',
        'Instagram': 'Instagram',
        'Facebook': 'Facebook',
        'TikTok': 'TikTok',
        'X (Twitter)': 'X (Twitter)',
        'Catppuccino Cafe': 'Catppuccino Kaviareň',
        'Catppuccino Café': 'Catppuccino Kaviareň',
        'Switch language': 'Prepnúť jazyk',
        'Toggle language to Slovak': 'Prepnúť jazyk na slovenčinu',
        'Toggle language to English': 'Prepnúť jazyk na angličtinu',
        'Prepnúť jazyk na angličtinu': 'Toggle language to English'
    };
const SK_TO_EN = Object.fromEntries(
    Object.entries(EN_TO_SK).map(([en, sk]) => [sk, en])
);
const EN_TO_SK_LOWER = Object.fromEntries(
    Object.entries(EN_TO_SK).map(([en, sk]) => [en.toLowerCase(), sk])
);
const SK_TO_EN_LOWER = Object.fromEntries(
    Object.entries(SK_TO_EN).map(([sk, en]) => [sk.toLowerCase(), en])
);

const textBaseline = new WeakMap();
const attrBaseline = new WeakMap();

function normalizeLang(value) {
    return String(value).toLowerCase() === 'sk' ? 'sk' : 'en';
}

function getCurrentLang() {
    return normalizeLang(localStorage.getItem(STORAGE_KEY) || DEFAULT_LANG);
}

function translateCore(text, targetLang) {
    const map = targetLang === 'sk' ? EN_TO_SK : SK_TO_EN;
    const lowerMap = targetLang === 'sk' ? EN_TO_SK_LOWER : SK_TO_EN_LOWER;

    if (map[text]) {
        return map[text];
    }

    const lower = text.toLowerCase();
    return lowerMap[lower] || text;
}

function translateKeepingWhitespace(source, targetLang) {
    if (!source || !source.trim()) {
        return source;
    }

    const leading = (source.match(/^\s*/) || [''])[0];
    const trailing = (source.match(/\s*$/) || [''])[0];
    const core = source.trim();

    return leading + translateCore(core, targetLang) + trailing;
}

function shouldSkipTextNode(node) {
    const parent = node.parentElement;
    if (!parent) {
        return false;
    }

    const tag = parent.tagName;
    if (tag === 'SCRIPT' || tag === 'STYLE' || tag === 'NOSCRIPT') {
        return true;
    }

    if (parent.closest('.cat-personality')) {
        return true;
    }

    return false;
}

function processTextNode(node, targetLang) {
    if (!node || shouldSkipTextNode(node)) {
        return;
    }

    if (!textBaseline.has(node)) {
        textBaseline.set(node, node.nodeValue || '');
    }

    const baseline = textBaseline.get(node) || '';
    const translated = translateKeepingWhitespace(baseline, targetLang);

    if (translated !== node.nodeValue) {
        node.nodeValue = translated;
    }
}

function translateTextNodes(root, targetLang) {
    const walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT);
    let node = walker.nextNode();

    while (node) {
        processTextNode(node, targetLang);
        node = walker.nextNode();
    }
}

function ensureAttrBaseline(element) {
    if (!attrBaseline.has(element)) {
        attrBaseline.set(element, {});
    }

    return attrBaseline.get(element);
}

function processElementAttributes(element, targetLang) {
    if (!(element instanceof Element)) {
        return;
    }

    const baselineMap = ensureAttrBaseline(element);

    TRANSLATABLE_ATTRS.forEach((attrName) => {
        const current = element.getAttribute(attrName);
        if (!current) {
            return;
        }

        if (!(attrName in baselineMap)) {
            baselineMap[attrName] = current;
        }

        const translated = translateKeepingWhitespace(baselineMap[attrName], targetLang);
        if (translated !== current) {
            element.setAttribute(attrName, translated);
        }
    });
}

function translateAttributes(root, targetLang) {
    if (root === document) {
        const all = document.querySelectorAll('[placeholder], [aria-label], [title]');
        all.forEach((element) => processElementAttributes(element, targetLang));
        return;
    }

    if (!(root instanceof Element)) {
        return;
    }

    processElementAttributes(root, targetLang);
    const descendants = root.querySelectorAll('[placeholder], [aria-label], [title]');
    descendants.forEach((element) => processElementAttributes(element, targetLang));
}

function updateToggleButton(lang) {
    const button = document.getElementById('lang-toggle-btn');
    if (!button) {
        return;
    }

    button.textContent = lang.toUpperCase();
    button.setAttribute('data-lang', lang);
    button.setAttribute(
        'aria-label',
        lang === 'en' ? 'Toggle language to Slovak' : 'Toggle language to English'
    );
    button.setAttribute('title', 'Switch language');
}

function applyLanguage(lang) {
    const targetLang = normalizeLang(lang);
    const root = document.body || document.documentElement;

    if (!root) {
        return;
    }

    root.classList.add('lang-transitioning');
    window.setTimeout(() => {
        root.classList.remove('lang-transitioning');
    }, TRANSITION_MS);

    document.documentElement.lang = targetLang;

    if (targetLang === 'sk') {
        document.documentElement.classList.add('lang-sk-active');
    } else {
        document.documentElement.classList.remove('lang-sk-active');
    }

    document.title = targetLang === 'sk' ? 'Catppuccino Kaviareň' : 'Catppuccino Café';

    translateAttributes(document, targetLang);
    translateTextNodes(root, targetLang);
    updateToggleButton(targetLang);
}

function toggleLanguage() {
    const next = getCurrentLang() === 'en' ? 'sk' : 'en';
    localStorage.setItem(STORAGE_KEY, next);
    applyLanguage(next);
}

function observeDynamicContent() {
    const observer = new MutationObserver((mutations) => {
        const currentLang = getCurrentLang();

        mutations.forEach((mutation) => {
            if (mutation.type === 'characterData') {
                processTextNode(mutation.target, currentLang);
                return;
            }

            if (mutation.type === 'attributes') {
                processElementAttributes(mutation.target, currentLang);
                return;
            }

            mutation.addedNodes.forEach((addedNode) => {
                if (addedNode.nodeType === Node.TEXT_NODE) {
                    processTextNode(addedNode, currentLang);
                    return;
                }

                if (addedNode.nodeType !== Node.ELEMENT_NODE) {
                    return;
                }

                translateAttributes(addedNode, currentLang);
                translateTextNodes(addedNode, currentLang);
            });
        });
    });

    if (document.body) {
        observer.observe(document.body, {
            childList: true,
            subtree: true,
            characterData: true,
            attributes: true,
            attributeFilter: TRANSLATABLE_ATTRS
        });
    }
}

function bindToggle() {
    const button = document.getElementById('lang-toggle-btn');
    if (!button) {
        return;
    }

    button.addEventListener('click', toggleLanguage);
}

function init() {
    bindToggle();
    applyLanguage(getCurrentLang());
    observeDynamicContent();
}

if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
} else {
    init();
}
})();